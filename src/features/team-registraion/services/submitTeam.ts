import { TeamRegistrationPayload } from '../types/team'

export async function submitTeam(data: TeamRegistrationPayload) {
  // Supabase integration removed
  console.log('Team submission disabled', data)
}
