import { SchoolRegistrationPayload } from '../types/school'

export async function submitSchool(data: SchoolRegistrationPayload) {
  // Supabase integration removed
  console.log('School submission disabled', data)
}
